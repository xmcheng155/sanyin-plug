# 阶段更新说明

## 2026-07-26：增加场景自动启动

- 场景新增自动启动开关、`HH:MM` 时间和星期选择，提供每天、工作日、周末快捷配置及自定义星期；
- 自动启动由音箱端常驻调度器按设备时区执行，关闭网页或更换访问设备不影响；
- 场景卡片展示自动启动摘要、下次执行时间和上次成功/失败结果，页面每 30 秒同步一次计划状态；
- 同一时间且星期有重叠的启用计划会被拒绝，避免多个场景争抢播放器；
- 触发标记会在播放前以 `0600` 权限持久化，服务重启后不会在同一分钟重复播放；错过的时间不补播，避免恢复服务后意外出声；
- 增加设备时区解析、下次执行计算、计划冲突、防重复执行、跨日再次执行及 API 错误契约测试。

## 2026-07-26：增加持久化播放器场景

- 播放页新增场景卡片，可创建、编辑、删除并在影响预览后启动场景；
- 每个场景包含名称、六类图标、媒体 URL、显示标题、0–100 本地音量和 0–60 分钟定时，最多保存 20 个；
- 场景写入音箱自有配置 `player-scenes.json`，换浏览器访问仍然保留；
- 完整媒体 URL 仅存储在设备权限为 `0600` 的配置中，API 和页面继续删除查询参数和用户信息；编辑时可留空保留完整私密地址；
- 启动场景会串行替换当前播放、回读场景音量、设置或取消定时，并返回最终播放器状态；
- 增加场景 CRUD、持久化、脱敏、完整应用及 Mock 写入保护测试。

## 2026-07-26：优化本地播放音量调节

- 音量滑杆改为拖动时即时反馈并每 150 毫秒合并目标值，不再等待、禁用或重建控件；
- 设备响应较慢时只保留最新音量，前一个请求完成后自动继续同步最终目标；
- 播放器状态轮询、自动续播和控制事务改为串行访问 KPlayer，避免音量写入与并发 SOAP 请求相互干扰；
- 继续通过 `GetVolume` 回读验收最终结果，失败时重新读取设备实际值。

## 2026-07-26：增加 SSH 运维与签名网页更新

- `sanyinctl` 新增 SSH 传输参数，检查、安装、备份、认证和重启命令可通过公钥 SSH 执行；
- 新增 `ssh-enable`，通过首次 ADB 连接写入公钥；固件有 Dropbear 时进行安全配置，否则安装仅支持 root 公钥、拒绝转发和 PTY 的三音 SSH 服务；
- SSH 运维与调试支持独立 `known_hosts`，并完成公钥拒绝、实机重启持久化和 UDISK 启动竞态修复；
- 增加 `-mode ssh` / `npm run dev:ssh` 真实设备开发模式，远端自有配置读写限制在项目配置目录；
- 应用构建写入版本、提交和构建时间，网页增加“版本与更新”页面；
- 新增 Ed25519 更新签名工具与 `npm run package:update -- X.Y.Z`；
- 网页只接收签名 `.sanyin-update`，并验证三文件白名单、签名、SHA-256、递增版本和 Linux/ARMv7 ELF；
- 设备端更新使用同分区原子替换，重启后检查进程和 TCP 8787，失败自动恢复上一版；
- 已在实机验证无效包拒绝、1.8.0→1.8.1 网页升级、故障 1.8.2 自动回滚，以及正式 1.8.2 升级；
- 网页更新只替换内嵌网页的单文件服务；启动项与辅助脚本仍通过 SSH/ADB 完整安装。

## 2026-07-25：从 AirPlay 恢复工具升级为三音 Plug

本阶段完成了项目定位和能力范围的扩展。最初版本只解决三音云音箱在云端能力失效后 AirPlay 端口无法持续使用的问题；当前版本已经成为运行在音箱本机的局域网增强与控制服务。

### 新的产品形态

- 增加 ARMv7 单文件服务 `sanyin-config`，直接安装到音箱；
- 增加 OpenWrt/procd 启动项，音箱重启后自动运行；
- 增加移动端优先的局域网页面，访问 `http://音箱IP:8787/`；
- 增加 `/api/v1` HTTP API、SSE 状态事件和 OpenAPI 3.1 契约；
- 增加 Mock 与真实设备两种运行模式，前端共用同一契约；
- 网页默认不设置密码，保留可选 Basic Auth，同源和 CSRF 检查始终启用。

### AirPlay 增强

- 保留原有 AirPlay 恢复守护服务；
- 在网页中展示 SPlayer、TCP 5002 和恢复服务状态；
- 增加手动恢复入口；
- 增加可持久化的自动恢复开关；
- 恢复成功必须通过端口回读验收，不以命令发送成功代替设备成功。

### 网络能力

- 展示当前 Wi-Fi 连接、SSID 和脱敏信号等级；
- 支持从网页切换 Wi-Fi；
- 切换前备份原配置；
- 45 秒内未同时确认目标 SSID、连接状态、IPv4、默认路由和网关可达时自动回退；
- 服务启动时能够恢复未完成的 Wi-Fi 事务，降低断电或重启导致失联的风险。

### 音频和蓝牙

- 支持请求开启或关闭蓝牙，并等待原厂成功事件；
- 当前蓝牙实时状态缺少无副作用查询，因此单独展示最近验收结果，不伪装成实时开关；
- 支持固件定义的七种 EQ 模式；
- 分开展示业务选中态、硬件已应用态和等待本地播放状态；
- EQ 写入通过音箱 loopback 原厂路由执行，并结合事件与硬件文件回读。

### 替代云端播放

- 接入原厂 KPlayer 的 UPnP AVTransport；
- 支持音箱直接拉取 HTTP/HTTPS 音频 URL；
- 支持播放、暂停、恢复、停止和下一首；
- 支持通过 KPlayer `RenderingControl` 在 0–100 范围内调节本地播放音量，并以 `GetVolume` 回读验收；
- 支持设备端播放队列和普通曲目自动续播；
- 支持收藏、播放、入队和删除网络电台；
- 支持通过上移、下移调整电台顺序；电台收藏及顺序写入自有配置，服务重启后保留；
- 通过 KPlayer `GetMediaInfo.CurrentURI` 恢复服务重启后的当前媒体名称；
- 针对直播流延长首次缓冲验收并归一化原厂伪时长、伪停止状态；
- 增加 1–60 分钟定时停止，倒计时由音箱执行，不依赖浏览器保持打开。

### 安全与隐私

- 不直接修改厂商 SQLite；
- 不修改 bootloader、内核或只读 SquashFS；
- 不向 API 暴露 BSSID、MAC、IP、Wi-Fi 密码、设备标识、令牌和原始云端日志；
- 对外返回媒体来源时去除用户信息、查询参数和片段；
- 密码和 Wi-Fi 临时数据使用限制权限，并在事务结束后清理；
- 构建产物、ADB、NAND 备份和原始协议抓包均由 `.gitignore` 排除。

### 已完成的实机验证

- AirPlay 安装、端口恢复、开机自动恢复和网页手动恢复；
- Wi-Fi 当前连接读取、成功切换、超时回退和启动恢复；
- 蓝牙开启与关闭事件；
- EQ 业务选中态及 KPlayer 播放时 Normal/Vocal 硬件切换；
- URL 播放、进度、暂停、恢复、停止、手动下一首和自动续播；
- 网络电台保存、播放、入队、删除和重启持久化；
- 播放 FM101.1 后 1 分钟定时停止；60 分钟上限接受，61 分钟拒绝；
- 服务重启后清除旧定时器，同时保留 15 个电台收藏；
- 默认无密码和可选 Basic Auth 两种启动方式。

### 仍属于实验能力的部分

Wi-Fi、蓝牙、EQ 和本地播放虽然已经完成正常路径实机验收，仍未覆盖所有路由器、媒体格式、断网、断电、KPlayer 重启以及 AirPlay/蓝牙/语音播报抢占场景，因此继续标记为 `experimental`。

音量、静音、灯光、麦克风计划、闹钟和提醒尚未达到可可靠读取、验收和恢复的标准，当前不开放写入。

### 文档变化

- README 已从 AirPlay 单功能说明改为完整项目入口；
- 新增 [三音 Plug 使用手册](docs/09-user-guide.md)；
- 原 [AirPlay 操作与恢复手册](docs/03-operations-and-recovery.md) 明确为 AirPlay 专项文档；
- 历史实施说明保留为设计与阶段记录，不再承担最终用户指南职责。
