# 阶段更新说明

## 2026-07-25：从 AirPlay 恢复工具升级为三音 Plug

本阶段完成了项目定位和能力范围的扩展。最初版本只解决三音云音箱在云端能力失效后 AirPlay 端口无法持续使用的问题；当前版本已经成为运行在音箱本机的局域网增强与控制服务。

### 新的产品形态

- 增加 ARMv7 单文件服务 `sanyin-config`，直接安装到音箱；
- 增加 OpenWrt/procd 启动项，音箱重启后自动运行；
- 增加移动端优先的局域网页面，访问 `http://音箱IP:8787/`；
- 增加 `/api/v1` HTTP API、SSE 状态事件和 OpenAPI 3.1 契约；
- 增加 Mock 与真实设备两种运行模式，前端共用同一契约；
- 网页默认不设置密码，保留可选 Basic Auth，同源和 CSRF 检查始终启用。

### AirPlay 增强

- 保留原有 AirPlay 恢复守护服务；
- 在网页中展示 SPlayer、TCP 5002 和恢复服务状态；
- 增加手动恢复入口；
- 增加可持久化的自动恢复开关；
- 恢复成功必须通过端口回读验收，不以命令发送成功代替设备成功。

### 网络能力

- 展示当前 Wi-Fi 连接、SSID 和脱敏信号等级；
- 支持从网页切换 Wi-Fi；
- 切换前备份原配置；
- 45 秒内未同时确认目标 SSID、连接状态、IPv4、默认路由和网关可达时自动回退；
- 服务启动时能够恢复未完成的 Wi-Fi 事务，降低断电或重启导致失联的风险。

### 音频和蓝牙

- 支持请求开启或关闭蓝牙，并等待原厂成功事件；
- 当前蓝牙实时状态缺少无副作用查询，因此单独展示最近验收结果，不伪装成实时开关；
- 支持固件定义的七种 EQ 模式；
- 分开展示业务选中态、硬件已应用态和等待本地播放状态；
- EQ 写入通过音箱 loopback 原厂路由执行，并结合事件与硬件文件回读。

### 替代云端播放

- 接入原厂 KPlayer 的 UPnP AVTransport；
- 支持音箱直接拉取 HTTP/HTTPS 音频 URL；
- 支持播放、暂停、恢复、停止和下一首；
- 支持通过 KPlayer `RenderingControl` 在 0–100 范围内调节本地播放音量，滑杆松手后立即设置，并以 `GetVolume` 回读验收；
- 支持设备端播放队列和普通曲目自动续播；
- 支持收藏、播放、入队和删除网络电台；
- 支持通过上移、下移调整电台顺序；电台收藏及顺序写入自有配置，服务重启后保留；
- 通过 KPlayer `GetMediaInfo.CurrentURI` 恢复服务重启后的当前媒体名称；
- 针对直播流延长首次缓冲验收并归一化原厂伪时长、伪停止状态；
- 增加 1–60 分钟定时停止，倒计时由音箱执行，不依赖浏览器保持打开。

### 安全与隐私

- 不直接修改厂商 SQLite；
- 不修改 bootloader、内核或只读 SquashFS；
- 不向 API 暴露 BSSID、MAC、IP、Wi-Fi 密码、设备标识、令牌和原始云端日志；
- 对外返回媒体来源时去除用户信息、查询参数和片段；
- 密码和 Wi-Fi 临时数据使用限制权限，并在事务结束后清理；
- 构建产物、ADB、NAND 备份和原始协议抓包均由 `.gitignore` 排除。

### 已完成的实机验证

- AirPlay 安装、端口恢复、开机自动恢复和网页手动恢复；
- Wi-Fi 当前连接读取、成功切换、超时回退和启动恢复；
- 蓝牙开启与关闭事件；
- EQ 业务选中态及 KPlayer 播放时 Normal/Vocal 硬件切换；
- URL 播放、进度、暂停、恢复、停止、手动下一首和自动续播；
- 网络电台保存、播放、入队、删除和重启持久化；
- 播放 FM101.1 后 1 分钟定时停止；60 分钟上限接受，61 分钟拒绝；
- 服务重启后清除旧定时器，同时保留 15 个电台收藏；
- 默认无密码和可选 Basic Auth 两种启动方式。

### 仍属于实验能力的部分

Wi-Fi、蓝牙、EQ 和本地播放虽然已经完成正常路径实机验收，仍未覆盖所有路由器、媒体格式、断网、断电、KPlayer 重启以及 AirPlay/蓝牙/语音播报抢占场景，因此继续标记为 `experimental`。

音量、静音、灯光、麦克风计划、闹钟和提醒尚未达到可可靠读取、验收和恢复的标准，当前不开放写入。

### 文档变化

- README 已从 AirPlay 单功能说明改为完整项目入口；
- 新增 [三音 Plug 使用手册](docs/09-user-guide.md)；
- 原 [AirPlay 操作与恢复手册](docs/03-operations-and-recovery.md) 明确为 AirPlay 专项文档；
- 历史实施说明保留为设计与阶段记录，不再承担最终用户指南职责。
